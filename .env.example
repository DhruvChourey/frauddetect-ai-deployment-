# Railway Environment Variables Configuration
# Copy these variables to Railway's environment variable settings

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (set to your frontend domain or * for all)
CORS_ORIGIN=*

# AI Provider Selection (mock, gemini, openai, or gemma)
PROVIDER=gemini

# Gemini API Configuration (Google AI Studio)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# OpenAI API Configuration (Optional - if using OpenAI provider)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Gemma/Hugging Face Configuration (Optional - if using Gemma provider)
GEMMA_API_KEY=your_huggingface_token_here
GEMMA_MODEL=google/gemma-3-2b
GEMMA_ENDPOINT=https://api-inference.huggingface.co/models/google/gemma-3-2b

# For Testing Without API Keys (Development Only)
# USE_MOCK=true
